import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Next.js requires this but NextConfig types might be outdated
  allowedDevOrigins: ['10.0.0.5']
};

export default nextConfig;
